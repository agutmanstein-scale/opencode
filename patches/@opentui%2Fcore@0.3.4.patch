diff --git a/index-54s7pk0d.js b/index-54s7pk0d.js
index 4558c02f59c2d6772f3615bd63e0768bc3f8e3f7..7495ba7b6c2cab7d2f064fab4885266debefa67a 100644
--- a/index-54s7pk0d.js
+++ b/index-54s7pk0d.js
@@ -7016,7 +7016,7 @@ var env = new Proxy({}, {
 
 // src/lib/stdin-parser.ts
 import { Buffer as Buffer3 } from "buffer";
-var DEFAULT_TIMEOUT_MS = 20;
+var DEFAULT_TIMEOUT_MS = 25;
 var DEFAULT_MAX_PENDING_BYTES = 64 * 1024;
 var INITIAL_PENDING_CAPACITY = 256;
 var ESC = 27;
@@ -7352,6 +7352,7 @@ class StdinParser {
   pendingSinceMs = null;
   forceFlush = false;
   justFlushedEsc = false;
+  justFlushedEscBracket = false;
   state = { tag: "ground" };
   cursor = 0;
   unitStart = 0;
@@ -7549,6 +7550,15 @@ class StdinParser {
             }
             this.justFlushedEsc = false;
           }
+          if (this.justFlushedEscBracket) {
+            if (byte === 60) {
+              this.justFlushedEscBracket = false;
+              this.cursor += 1;
+              this.state = { tag: "esc_less_mouse" };
+              continue;
+            }
+            this.justFlushedEscBracket = false;
+          }
           if (byte === ESC) {
             this.cursor += 1;
             this.state = { tag: "esc" };
@@ -7675,7 +7685,8 @@ class StdinParser {
               this.markPending();
               return;
             }
-            this.emitKeyOrResponse("unknown", decodeUtf8(bytes.subarray(this.unitStart, this.cursor)));
+            this.emitOpaqueResponse("unknown", bytes.subarray(this.unitStart, this.cursor));
+            this.justFlushedEscBracket = true;
             this.state = { tag: "ground" };
             this.consumePrefix(this.cursor);
             continue;
@@ -7702,6 +7713,7 @@ class StdinParser {
               return;
             }
             this.emitOpaqueResponse("unknown", bytes.subarray(this.unitStart, this.cursor));
+            this.justFlushedEscBracket = true;
             this.state = { tag: "ground" };
             this.consumePrefix(this.cursor);
             continue;
@@ -8233,10 +8245,9 @@ class StdinParser {
               this.markPending();
               return;
             }
-            this.emitOpaqueResponse("unknown", bytes.subarray(this.unitStart, this.cursor));
-            this.state = { tag: "ground" };
-            this.consumePrefix(this.cursor);
-            continue;
+            this.pendingSinceMs = null;
+            this.forceFlush = false;
+            return;
           }
           if (byte >= 48 && byte <= 57 || byte === 59) {
             this.cursor += 1;
@@ -8444,6 +8455,7 @@ class StdinParser {
     this.pendingSinceMs = null;
     this.forceFlush = false;
     this.justFlushedEsc = false;
+    this.justFlushedEscBracket = false;
     this.state = { tag: "ground" };
     this.cursor = 0;
     this.unitStart = 0;
@@ -24923,9 +24935,14 @@ Captured external output:
       explicitWidthCprActive: false,
       startupCursorCprActive: false
     }, true);
+    const mouseWasEnabled = this._useMouse;
     this._useMouse = false;
     this.setCapturedRenderable(undefined);
+    if (mouseWasEnabled) {
+      this.disableMouse();
+    }
     this.stdin.removeListener("data", this.stdinListener);
+    while (this.stdin.read() !== null) {}
     if (this.stdin.setRawMode) {
       try {
         this.stdin.setRawMode(false);
